spring.application.name=notification-service
server.port=8083

logging.level.com.bankapp=DEBUG

# Same shared dev secret as every other service (see core-banking/identity-service
# application.properties) - all services must agree on it to interoperate.
jwt.secret=Y2hhbmdlVGhpc0luUHJvZHVjdGlvbkJhbmtBcHBTZWNyZXRLZXkyMDI0

spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:d94064dfca2847fdde6b3bd5c81e7873}

# Dev-only root credentials, matching docker-compose's MONGO_INITDB_ROOT_USERNAME/PASSWORD -
# authSource=admin because MONGO_INITDB_ROOT_* creates the user in the admin db, not notifications.
spring.data.mongodb.uri=mongodb://bankapp:0ca39c46e075fcc9b4b5446eeab90e9f@${MONGO_HOST:localhost}:${MONGO_PORT:27017}/notifications?authSource=admin

# Used only by MessageSeeder at startup to resolve the seeded demo users' ids.
identity.service.url=${IDENTITY_SERVICE_URL:http://localhost:8081}

management.endpoints.web.exposure.include=health
management.endpoint.health.enabled=true
